apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bank-frontend-ingress
  namespace: bank-app
  annotations:
    cert-manager.io/cluster-issuer: http-01-production
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - bank.gwinseapptest.online
      secretName: bank-gwinseapptest-online
  rules:
    - host: bank.gwinseapptest.online
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bank-frontend-service
                port:
                  number: 80
